{
  "name": "hitokoto-renderer",
  "version": "2.0.2",
  "type": "commonjs",
  "scripts": {
    "build": "tsc ./build.mts && node ./build.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "esbuild": "^0.25.0",
    "typescript": "^7.0.0"
  }
}
